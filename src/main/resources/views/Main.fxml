<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns:fx="http://javafx.com/fxml" prefWidth="700" prefHeight="550">
    <top>
        <VBox spacing="10" BorderPane.alignment="CENTER">
            <children>
                <Label text="Sistema de Notificaciones" style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: #5E4B8B;" />
                <Separator />
            </children>
            <padding>
                <Insets top="20" bottom="10" />
            </padding>
        </VBox>
    </top>

    <center>
        <VBox spacing="15" alignment="TOP_LEFT">
            <children>

                <HBox spacing="10">
                    <Label text="Seleccionar Usuario:"/>
                    <ComboBox fx:id="comboUsuario" prefWidth="200"/>
                    <CheckBox fx:id="checkBloqueado" text="Usuario Bloqueado"/>
                </HBox>

                <HBox spacing="10">
                    <Label text="Canal de Notificación:"/>
                    <ComboBox fx:id="comboCanal" prefWidth="200"/>
                </HBox>

                <Label text="Mensaje:"/>
                <TextArea fx:id="textoMensaje" prefRowCount="4" wrapText="true"/>

                <HBox spacing="10" alignment="CENTER_LEFT">
                    <CheckBox fx:id="checkValidarMensaje" text="Validar mensaje no vacío" selected="true"/>
                    <CheckBox fx:id="checkGuardarHistorial" text="Guardar en historial" selected="true"/>
                </HBox>

                <Button fx:id="botonEnviar" text="📤 Enviar Notificación" style="-fx-background-color: #5E4B8B; -fx-text-fill: white;" prefWidth="200"/>

                <Label text="Historial de Notificaciones:"/>
                <ListView fx:id="listaHistorial" prefHeight="150"/>

            </children>
            <padding>
                <Insets top="20" right="30" bottom="20" left="30"/>
            </padding>
        </VBox>
    </center>

</BorderPane>
